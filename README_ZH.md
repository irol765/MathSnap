
[🇺🇸 English Readme](README.md)

# StudySnap (学习拍 - AI 全科导师)

**StudySnap** 是一个基于 **Google Gemini 3 Pro** 模型的全科智能辅导 Web 应用。无论是数学公式、物理定律还是历史事件，拍照即可获得详细解析与互动测验。

## 核心特性

*   **全科支持**：覆盖数理化生政史地等所有学科。
*   **双模式 API 支持**：自动识别配置，支持 Google 原生协议或 OpenAI 兼容协议（完美适配 NewAPI/OneAPI）。
*   **智能图像识别**：精准解析题目图片。
*   **互动测验**：基于题目生成变式选择题，巩固知识点。
*   **PWA 支持**：精美的 iOS 风格图标，支持添加到主屏幕。

## API 配置策略（重要）

本项目根据环境变量自动切换底层调用方式：

### 模式 A：Google 原生模式（默认）
使用 Google 官方 SDK。适合网络环境能直接连接 Google 的用户。
*   **API_KEY**: 填入 Google AI Studio 的 Key。
*   **API_BASE_URL**: **留空**（不要填写）。

### 模式 B：OpenAI 兼容模式 (NewAPI/OneAPI)
使用 OpenAI 的接口格式 (`/v1/chat/completions`) 进行调用。适合使用中转分发（如 NewAPI、OneAPI）的用户，因为很多中转系统不支持 Google 原生 SDK 格式。
*   **API_KEY**: 填入你的中转 Key (通常以 `sk-` 开头)。
*   **API_BASE_URL**: **必填**。填入中转地址（例如 `https://api.xyhelper.cn/v1` 或 `https://your-domain.com`）。
    *   *原理：一旦检测到此变量，系统将放弃 Google SDK，改用标准 fetch 请求 OpenAI 格式接口。*

## 部署 (Vercel)

1.  **Fork** 本仓库。
2.  在 Vercel 中 **Import**。
3.  配置 **Environment Variables** (环境变量)：
    *   `API_KEY`: 必填。
    *   `API_BASE_URL`: **如果是中转/NewAPI 用户，必填**。
    *   `ACCESS_CODE`: 选填（访问密码）。
4.  点击 **Deploy**。

### ⚠️ 修改配置后需重新部署
如果你在部署后修改了 `API_BASE_URL`，必须在 Vercel 控制台手动点击 **Redeploy**，否则新配置不会生效。

## 手机安装 (PWA)

为了获得最佳体验（类似原生 App），请添加到主屏幕：
*   **iOS**: Safari -> 分享按钮 -> 添加到主屏幕。
*   **Android**: Chrome -> 菜单 -> 安装应用。

---
Powered by Gemini Models
